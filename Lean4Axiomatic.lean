-- Import all files in the project here so that they all get compiled when
-- running `lake build`. Imports are transitive, so only a few statements are
-- actually needed.
import Lean4Axiomatic.Integer.Impl.Difference
import Lean4Axiomatic.Natural.Impl.Nat
import Lean4Axiomatic.Rational.Impl.Fraction
import Lean4Axiomatic.Sequence

def hello := "world"
